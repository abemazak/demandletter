// Import from original strategyGuide temporarily
import { strategyGuide as originalStrategy } from '../strategyGuide';

// Export the original strategy
export const strategyGuide = originalStrategy;

// In the future, this will be replaced with individual imports:
// import { caseStrategy } from './caseStrategy';
// import { negotiationLeverage } from './negotiationLeverage';
// etc.

export default strategyGuide; 